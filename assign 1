#include <iostream>
using namespace std;

int main()
{
    int a, b;
    cin >> a;
    cin >> b;

    if (a == b)
    {
        cout << "a and b are equal.";
    }
    else if (a > b)
    {
        cout << "a is greater.";
    }
    else
    {

        cout << "b is greater.";
    }
}
